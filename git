mkdir web-project
cd web-project
git init

echo "<!DOCTYPE html>
<html>
<head>
    <title>Web Project</title>
</head>
<body>
    <h1>Welcome to My Web Project</h1>
</body>
</html>" > index.html

git add index.html
git commit -m "Initial project structure"

git branch -M main

git checkout -b feature-style

echo "body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
}
h1 {
    color: blue;
}" > style.css

git add style.css
git commit -m "Add stylesheet"

git checkout main

git merge feature-style

git log --oneline --graph --all

git remote add origin https://github.com/<your-username>/<your-repository>.git

git push -u origin main

Required Screenshots:

Screenshot 1:
- Terminal showing:
  git init
  git add index.html
  git commit -m "Initial project structure"

Screenshot 2:
- Terminal showing:
  git checkout -b feature-style
  git add style.css
  git commit -m "Add stylesheet"

Screenshot 3:
- Terminal showing:
  git checkout main
  git merge feature-style
  or
  git log --oneline --graph --all

Screenshot 4:
- Terminal showing:
  git push -u origin main
  with the message indicating the push completed successfully.
